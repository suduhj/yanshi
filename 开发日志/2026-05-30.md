# 2026-05-30 开发日志

## 完成事项
- 接入 DeepSeek Chat Completions 调用入口，支持 JSON 输出和可配置模型。
- 新增自然语言解析单个任务草稿能力，解析结果通过现有任务校验和中国时间工具归一化。
- 新增任务表单增加“自然语言描述”和“解析填入”，AI 只回填草稿，不直接创建任务。
- 增加 DeepSeek wrapper、任务草稿解析和表单状态测试。
- 接入本地轻量提醒建议，首页展示逾期/今天截止、每日任务未完成和持续推进任务缺少下一步动作提醒。
- 将默认端口从 3000 调整为 3100，避开 `music_aagent` 项目占用的 3000 端口。
- 接入浏览器通知提醒，用户授权后页面打开期间对当天未通知过的提醒发送通知。
- 增加通知去重工具测试，按中国日期键避免同一天重复通知。

## 待办事项
- 后续如需接入后台常驻 Agent、Web Push 或系统级定时提醒，应作为第一阶段之外的新里程碑重新确认范围。

## 文档更新
- 更新 `.env.example`，补充 `DEEPSEEK_MODEL`。
- 更新需求、技术、设计、执行步骤和 GPT 协作说明，记录 DeepSeek 解析已接入和 Agent 仍预留。
- 更新需求、技术、设计、执行步骤和 GPT 协作说明，记录本地轻量提醒已接入。
- 更新 README，默认访问地址改为 `http://localhost:3100`。
- 更新需求、技术、设计、执行步骤和 GPT 协作说明，记录浏览器通知提醒已接入。

## 备注
- 基线测试时 `corepack pnpm test` 触发 pnpm 重建 `node_modules`，首次在非交互模式中断；已通过授权执行 `corepack pnpm install --frozen-lockfile` 恢复依赖，安装复用本地 store。
