# 2026-05-31 开发日志

## 完成事项
- 修复开发日志脚本的日期生成逻辑，改为按中国日期键创建或更新当天日志。
- 为日志脚本新增日期键测试，覆盖北京时间凌晨对应 UTC 前一天下午的场景。
- 明确当前不维护单独 `tasks/` 任务目录，具体任务以用户对话和每日开发日志为准。
- 调整 2026-05-30 待办措辞，避免与第一阶段“不接入后台常驻 Agent、Web Push 或系统级定时能力”的范围冲突。
- 增加动态“待确认”分区，截止时间已过但未确认的任务先进入确认流程。
- 增加“晨间回砚”确认面板，支持确认完成、加入今日、推迟截止时间和拆成下一步。
- 补齐任务卡片核心处理操作：完成、完成今日、今日推进完成、更新下一步、推迟和标记整个任务完成。
- 顶部统计卡片改为可点击入口，提醒建议可跳转并高亮具体任务。
- 顶部统计补充“其他任务”入口，支持查看其他任务分类数量和空状态。
- 调整任务卡片按钮层级，弱化删除按钮，压缩空状态区域。
- 调整每日任务按钮文案，未完成时显示“完成今日”；进一步弱化删除按钮默认视觉。
- 补充 README 第一阶段能力、环境变量、DeepSeek 配置、Docker 数据卷和 Next.js 开发工具说明。
- 优化快捷操作输入反馈，推迟截止时间和更新下一步需要填写内容后再提交。
- 精简 DeepSeek 非 2xx 错误提示，避免把上游响应体直接展示给用户。
- 统一顶部统计入口文案，把“今天截止”调整为“今日必须完成”，与任务分区和文档保持一致。
- 新增 `TaskEvent` 复盘历史模型和迁移，用于记录任务完成、每日完成、今日推进、更新下一步、推迟和晨间确认相关动作。
- 首页新增紧凑的“今日小结”，展示本地规则小结和“生成 AI 回顾”入口。
- 新增 AI 回顾编排逻辑，DeepSeek 不可用时回退到本地小结，且不直接修改任务。
- 任务编辑页新增“最近记录”，展示当前任务的复盘历史。
- 修复代码审查发现的问题：AI 回顾改用 DeepSeek 文本输出；任务动作和复盘事件写入改为同一个 Prisma transaction。

## 待办事项
- 暂无。

## 文档更新
- 更新 `GPT.md`，补充当前不维护单独 `tasks/` 任务目录的协作说明。
- 更新 `开发日志/2026-05-30.md`，明确后台常驻 Agent、Web Push 和系统级定时提醒属于后续新里程碑。
- 更新需求、技术、设计和执行步骤文档，记录待确认、晨间回砚、卡片处理操作和跳转入口。
- 修正执行步骤中的提醒描述，把旧的“逾期/今天截止”统一为“待确认/今天截止”。
- 更新 README，补充第一阶段验收和本地运行说明。
- 新增第二阶段“复盘历史与 AI 回顾”设计规格和实现计划，明确 2.1 先做“今日小结闭环”。
- 更新需求、技术、设计和执行步骤文档，记录后续顺序为复盘历史与 AI 回顾、数据安全与迁移、提醒系统升级。
- 更新技术规范中的第二阶段迁移说明，记录 `20260531150807_add_task_events`。
- 更新技术规范，明确复盘事件事务写入和 AI 回顾文本输出约束。

## 备注
- “待确认”采用动态计算，不新增数据库迁移；第二阶段开始后，持续推进的今日推进完成会通过 `TaskEvent` 记录为复盘事件。
