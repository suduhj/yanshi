services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: file:./data/dev.db
      DEEPSEEK_API_KEY: ""
      DEEPSEEK_BASE_URL: https://api.deepseek.com
    volumes:
      - ./prisma/data:/app/prisma/data
